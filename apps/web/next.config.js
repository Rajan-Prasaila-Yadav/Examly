/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['pub-mock.r2.dev', 'lh3.googleusercontent.com'],
  },
};

module.exports = nextConfig;
